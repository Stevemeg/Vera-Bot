services:
  - type: web
    name: vera-bot
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /v1/healthz
    envVars:
      - key: VERA_PERSIST_PATH
        value: /srv/data/context_snapshot.json
